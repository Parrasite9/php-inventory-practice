# 01 · Create one item

**File:** `inventory.php`, under `YOUR PRACTICE AREA`.

The `Item` class is already defined. No item object has been created yet.

Example from another project:

```php
$lamp = new Lamp;
```

`Lamp` is an existing class. `new` creates an object; `$lamp` is the variable holding it. This example is not code to add here.

**Your turn:** create an object of our `Item` class. Store it in a variable named `$bolts`.

Stop and share that line for review. You do not need to add a method or print anything yet.
